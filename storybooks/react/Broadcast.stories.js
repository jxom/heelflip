import React from 'react';

import Broadcast from './Broadcast';

export default {
  title: 'Heelflip/React/Broadcast',
  component: Broadcast,
};

const Template = (args) => <Broadcast {...args} />;

export const Default = Template.bind({});
