import React from 'react';

import Basic from './Basic';

export default {
  title: 'Heelflip/React/Basic',
  component: Basic
};

const Template = (args) => <Basic {...args} />;

export const Default = Template.bind({});