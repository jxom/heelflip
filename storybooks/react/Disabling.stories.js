import React from 'react';

import Disabling from './Disabling';

export default {
  title: 'Heelflip/React/Disabling',
  component: Disabling,
};

const Template = (args) => <Disabling {...args} />;

export const Default = Template.bind({});
