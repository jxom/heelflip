import React from 'react';

import BasicDeferred from './BasicDeferred';

export default {
  title: 'Heelflip/React/Basic (deferred)',
  component: BasicDeferred,
};

const Template = (args) => <BasicDeferred {...args} />;

export const Default = Template.bind({});
