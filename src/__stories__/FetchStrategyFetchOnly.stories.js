import _InvokeStrategyFetchOnly from './FetchStrategyFetchOnly.svelte';

export default {
  title: 'Fetch strategy (fetch only)',
};

export const Default = () => ({
  Component: _InvokeStrategyFetchOnly,
});
